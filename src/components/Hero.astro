<section class="hero" class="section reveal">

  <div class="hero-overlay"></div>

  <div class="container hero-content">
    <h1>Bienvenido a mi Portafolio</h1>
    <p>Espacio en donde presentare mi progreso en proyectos digitales.</p>
    <a class="btn" href="#Presentation">Ver presentación</a>
  </div>
</section>

<style>

  .hero {
    position: relative;
    background: url("/src/assets/fondo2.jpg");
    background-size: cover;
    background-position: center;
    color: #f9fafb;
    padding: 130px 0;
    text-align: center;
    overflow: hidden;
  }

 
  .hero-overlay {
    position: absolute;
    inset: 0;
    background: rgba(2, 6, 23, 0.65);
    backdrop-filter: blur(2px);
  }


  .hero-content {
    position: relative;
    z-index: 2;
  }

  
  .container {
    width: 90%;
    max-width: 1100px;
    margin: auto;
  }

 
  .hero h1 {
    font-size: 2.8rem;
    font-weight: 600;
    letter-spacing: 0.5px;
    margin-bottom: 12px;
  }

  
  .hero p {
    font-size: 1.1rem;
    color: #cbd5e1;
    max-width: 640px;
    margin: auto;
  }

  
  .btn {
    display: inline-block;
    margin-top: 28px;
    padding: 12px 28px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;

    background: #686e7e;
    color: white;

    box-shadow: 0 6px 18px rgba(0, 0, 0, 0.35);
    transition: all 0.25s ease;
  }

  
  .btn:hover {
    background: #515563;
    transform: translateY(-2px);
    box-shadow: 0 12px 26px rgba(0, 0, 0, 0.45);
  }

  
  @media (max-width: 768px) {
    .hero {
      padding: 100px 0;
    }

    .hero h1 {
      font-size: 2.2rem;
    }
    .hero{
  position:relative;
  overflow:hidden;
}

.hero::after{
  content:"";
  position:absolute;
  inset:0;
  background:radial-gradient(circle at 20% 30%, rgba(99,102,241,0.15), transparent);
}
  }
</style>
