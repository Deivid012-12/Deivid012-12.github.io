<section class="smallcards" class="section reveal">
  <div class="smallcards-container">
    
    <a href="#projects" class="smallcard">
      <div class="icon">📁</div>
      <div>
        <h4>Projects</h4>
        <p>See my latest work and applications.</p>
      </div>
    </a>

    
    <a href="#about" class="smallcard">
      <div class="icon">👤</div>
      <div>
        <h4>About Me</h4>
        <p>Learn more about who I am.</p>
      </div>
    </a>

    <a href="#blog" class="smallcard">
      <div class="icon">💬</div>
      <div>
        <h4>Blog</h4>
        <p>Read my latest articles and tutorials.</p>
      </div>
    </a>

    
    <a href="#contact" class="smallcard">
      <div class="icon">✉️</div>
      <div>
        <h4>Contact</h4>
        <p>Get in touch with me easily.</p>
      </div>
    </a>
  </div>
</section>

<style>
  
  .smallcards {
    padding: 55px 0;
    background: linear-gradient(180deg, #020617, #0f172a);
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
  }

  
  .smallcards-container {
    width: 90%;
    max-width: 1100px;
    margin: auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, 240px);
    justify-content: center;
    gap: 20px;
  }

  
  .smallcard {
    position: relative;
    background: rgba(255, 255, 255, 0.03);
    border-radius: 12px;
    padding: 18px;
    display: flex;
    gap: 14px;
    align-items: center;
    text-decoration: none;

    border: 1px solid rgba(255, 255, 255, 0.06);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.45);
    backdrop-filter: blur(6px);

    transition: all 0.25s ease;
  }

  
  .smallcard::before {
    content: "";
    position: absolute;
    inset: 0;
    border-radius: 12px;
    background: radial-gradient(
      circle at top,
      rgba(59, 130, 246, 0.15),
      transparent 70%
    );
    opacity: 0;
    transition: opacity 0.25s ease;
  }

  .smallcard:hover {
    transform: translateY(-4px);
    box-shadow:
      0 0 0 1px rgba(99, 102, 241, 0.3),
      0 10px 25px rgba(0, 0, 0, 0.4),
      0 0 18px rgba(99, 102, 241, 0.25);
  }

  .smallcard:hover::before {
    opacity: 1;
  }

 
  .icon {
    font-size: 22px;
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.06);
    border-radius: 9px;
    box-shadow: inset 0 0 8px rgba(255, 255, 255, 0.04);
  }

 
  .smallcard h4 {
    margin: 0;
    font-size: 15px;
    color: #f1f5f9;
    font-weight: 600;
  }

  
  .smallcard p {
    margin: 2px 0 0;
    font-size: 13px;
    color: #cbd5e1;
  }

  
  @media (max-width: 768px) {
    .smallcards {
      padding: 45px 0;
    }
  }
</style>
